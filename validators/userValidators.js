const {
  validatePassword,
  validateStudentNumber,
  validateUserPayload,
  validateUsername
} = require('./authValidators');

module.exports = {
  validatePassword,
  validateStudentNumber,
  validateUserPayload,
  validateUsername
};
